{
  "name": "skill-icons",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "^20.13.1",
    "npm": "^10.5.2"
  },
  "scripts": {
    "build": "node ./build.js",
    "deploy": "wrangler deploy dist/index.js",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^3.56.0"
  }
}
